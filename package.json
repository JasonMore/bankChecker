{
  "name": "bankchecker",
  "version": "1.0.0",
  "description": "Log into your bank to check transactions, then send yourself an email",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cheerio": "0.20.0"
  }
}
